@model ComputerStore.Domain.Entities.Product

<div class="well">
    <h3>
        <strong>@Model.Brand</strong>
        <span class="pull-right label label-primary">@Model.Price.ToString("c")</span>
    </h3>
    <span class="lead">@Model.Model</span>
</div>